<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.emmmua.mapper.mainMapper">
    
    
    <update id="reUser">
        update user set url = #{user.url}, username = #{user.username}, password = #{user.password} where id = #{user.id};
    </update>
</mapper>